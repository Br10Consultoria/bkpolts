services:
  olt-backup:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: olt-backup
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - .:/app                           # raiz do projeto — git pull reflete imediatamente
      - ${DATACOM_BACKUP_SFTP_DIR:-backup-data}:/app/backups  # ver README (Servidor de backup Datacom)
      - backup-logs:/app/logs            # logs de execução (volume persistente)
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=America/Bahia
    network_mode: host                   # acesso à rede local das OLTs

volumes:
  backup-data:
    driver: local
  backup-logs:
    driver: local
